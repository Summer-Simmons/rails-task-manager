<h1>My Tasks</h1>

<ul>

  <% @tasks.each do |task|%>
    <li>
      <p>
        <%= task.completed ? '[✅]' : '[  ]' %>
        <strong> <%= task.title  %> </strong>
      </p>
    </li>
  <% end %>

</ul>
