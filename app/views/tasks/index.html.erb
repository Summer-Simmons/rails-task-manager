<h1>My Tasks</h1>

<ul>

  <% @tasks.each do |task|%>
    <li>
      <p>
        <%= task.completed ? '[✅]' : '[  ]' %>
        <strong> <%= task.title  %> </strong> : <%= task.details %>
      </p>
    </li>
  <% end %>

</ul>
